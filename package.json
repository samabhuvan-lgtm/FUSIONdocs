{
  "name": "fusiondocs",
  "version": "1.0.0",
  "private": true,
  "description": "FusionDocs - Document Generation Application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
